# Upstash Redis Configuration
# Choose between REST (recommended for serverless) or TCP (better performance)
UPSTASH_CONNECTION_TYPE=REST  # Options: REST or TCP

# For REST connection (recommended for serverless environments)
# Get these from your Upstash Redis dashboard at https://console.upstash.com/
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-redis-token

# For TCP connection (better performance, persistent connections)
# Format: rediss://[password]@[endpoint]:[port]
# Example: rediss://abc123xyz@busy-panda-8060.upstash.io:6379
UPSTASH_REDIS_URL=rediss://your-password@your-endpoint.upstash.io:6379

# Previous Valkey/Redis configuration (no longer needed)
# VALKEY_USERNAME=
# VALKEY_PASSWORD=
# VALKEY_HOST=
# VALKEY_PORT=

# Other existing environment variables
SUPABASE_URL=your-supabase-url
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
SESSION_SECRET=your-session-secret
PORT=3000

# Add your other environment variables here...